# Secrets — never commit. Real values live in secrets.env (see secrets.env.example).
secrets.env
secrets.*.env
*.pem
*.key

# Holds the live Google Maps API key. Commit the .example template instead.
sfdx-project/scripts/set_keyburn_settings.apex
sfdx-project/scripts/set_keyburn_config.apex

# Vendored third-party skills library: not project code
sf-skills-1.55.0/

# Kept locally, not published
docfiles/Builders Panel .pdf
docfiles/Agentforce_FDE_Panel_Prep.md

# Editor / Claude Code local state
.vscode/
.claude/

# Salesforce CLI local state
.sf/
.sfdx/
sfdx-project/.sf/
sfdx-project/.sfdx/

# Phase 11 voice page: per-call relay logs (copy any worth keeping into docs)
bedrock-voice/call_logs/

# Python
__pycache__/
*.pyc

# Eval harness scratch output. Keep the saved runs in src/eval_reports/ — those are demo evidence.
src/eval_report.json

<?xml version="1.0" encoding="UTF-8"?>
<CustomTab xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- The demo shows the verification record instead of the mailbox: this Developer Edition org
         sends without domain authentication, so the code is read from Code_Plain__c (test mode). -->
    <customObject>true</customObject>
    <motif>Custom53: Padlock</motif>
</CustomTab>
